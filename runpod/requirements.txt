# RunPod worker deps. Torch/CUDA come from the base image (see Dockerfile).
runpod>=1.7.0
audio-separator[gpu]>=0.28.0
boto3>=1.35.0
# DDSP timbre transfer (torchaudio is installed separately in the Dockerfile from the
# cu128 index to match the base torch 2.7.1):
omegaconf>=2.3.0
soundfile>=0.12.0
scipy>=1.11.0
